<Window x:Class="HelloWpf.Test"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HelloWpf"
        mc:Ignorable="d"
        Title="Test" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="txtTest" Grid.Row="0"></TextBlock>
        <TextBox x:Name="txtBoxChild" Grid.Row="1" Width="300" Height="40" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,10,0,0" ></TextBox>
        <Button Click="Button_Click" Grid.Row="1" Width="100" Height="20" Content="button" ></Button>
    </Grid>
</Window>
